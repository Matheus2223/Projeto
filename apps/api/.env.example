# --- Core ---
DATABASE_URL="postgresql://user:password@localhost:5432/trendhub?schema=public"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=3333
WEB_APP_URL="http://localhost:3000"

# --- AI ---
OPENAI_API_KEY=""
# How many ContentIdea rows the 07:00 daily pipeline generates per run
# (via OpenAI when OPENAI_API_KEY is set, or the local rule-based fallback
# otherwise — see src/ai/ideas-generation.service.ts).
DAILY_IDEAS_COUNT=12

# --- Integrations (see src/integrations/*.integration.ts for setup notes) ---
YOUTUBE_API_KEY=""
REDDIT_CLIENT_ID=""
REDDIT_CLIENT_SECRET=""
X_BEARER_TOKEN=""
INSTAGRAM_ACCESS_TOKEN=""
TIKTOK_API_KEY=""
